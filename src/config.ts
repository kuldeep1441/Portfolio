const CONFIG = {
  /** Set to false to remove the dot-grid background in one step */
  showDotGrid: true,

  /** Set to false to remove the subtle grain/noise texture in one step */
  showNoise: true,
};

export default CONFIG;
